/**
 * JSON-LD 构建工具统一导出
 * 提供课程和体系课的结构化数据构建功能
 */

export {
  buildCourseJsonLd,
  serializeJsonLd,
  generateJsonLdScript
} from './buildCourseJsonLd'

export {
  buildProgramJsonLd,
  serializeProgramJsonLd,
  generateProgramJsonLdScript
} from './buildProgramJsonLd'
